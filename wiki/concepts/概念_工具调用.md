---
type: concept
tags:
  - ai
  - agent
  - tool-calling
  - execution
summary: "Agent 通过工具调用将决策落地为实际操作，核心模式有极简 CLI 工具集和外部 API 调用两种路线。"
sources:
  - "raw/OpenClaw橙皮书_extracted.txt"
  - "raw/douyin/2026-05-14/抖音-视频-20260514-FunctionCalling.md"
updated: "2026-05-14"
---

# 概念：工具调用与执行

## 定义

工具调用是 Agent 将大模型的决策落地为实际操作的能力层。大模型负责"思考"，工具调用负责"动手"——没有工具调用，Agent 只是一个会说话的聊天机器人。

## 两种主流路线

### 路线一：极简 CLI 工具集（OpenClaw 模式）

核心只有 **4 个工具**，遵循 Unix 哲学：

| 工具 | 用途 | 说明 |
|------|------|------|
| Read | 读取文件 | 获取上下文信息 |
| Write | 写入文件 | 创建或修改文件 |
| Edit | 编辑文件 | 精确修改已有文件 |
| Bash | 执行命令 | 运行 CLI 程序、操作系统操作 |

**优势**：
- System prompt 极短，Token 消耗少
- 覆盖几乎所有操作系统级别的任务
- Agent 不依赖预构建工具，通过 CLI 自行扩展能力

**代价**：
- 需要更强的模型能力来"从零写工具"
- 推荐使用 Claude Opus 等高能力模型

### 路线二：MCP 协议（Anthropic 标准）

MCP（Model Context Protocol）是 Anthropic 提出的工具协议标准，多数 AI Agent 框架集成此方案。

**优势**：
- 标准化协议，生态丰富
- 预构建工具集，开箱即用

**代价**：
- 协议层开销，增加复杂度
- 依赖外部预构建工具

### OpenClaw 的反 MCP 立场

OpenClaw 故意不支持 MCP，创始人认为 CLI/Unix 才是真正能 scale 的方案。对于需要 MCP 的场景，通过内置的 `mcporter` skill 桥接。核心观点：**强制 Agent 自己扩展能力**，而非消费预构建的工具集。

## Agent 工作区文件结构

工具调用的执行环境以文件系统为基础：

```
workspace/
├── AGENTS.md          # Agent 定义（身份、行为规则）
├── SOUL.md            # 人格指令（不可变内核）
├── USER.md            # 用户信息与偏好
├── MEMORY.md          # 长期记忆存储
├── HEARTBEAT.md       # 心跳配置（定时任务）
├── memory/            # Daily Logs 目录
│   └── YYYY-MM-DD.md
├── skills/            # 本地技能目录
└── sessions.json      # 会话存储
```

所有配置为纯文本文件，人类可直接编辑。

## 执行闭环

典型的工具调用路径：

1. **感知**：接收用户输入和环境状态
2. **决策**：大模型分析后决定调用哪个工具
3. **执行**：通过工具执行实际操作（读文件、写文件、执行命令等）
4. **反馈**：将执行结果返回给模型，评估是否需要下一步
5. **循环**：未完成则继续决策-执行-反馈，直到任务闭环

## 关键设计原则

- 工具越少，system prompt 越短，Token 消耗越低
- Agent 应有自我扩展能力——遇到不会的操作时自行编写工具
- CLI 是最通用的接口层，不需要为每个服务写独立集成
- 工具执行结果应只保留关键片段，避免海量原始数据塞入上下文

## Function Calling 机制

工具调用的底层技术实现是 **Function Calling**——LLM 通过函数声明自动决定调用哪个工具、生成参数 JSON。

详见 [[concepts/概念_FunctionCalling|Function Calling 详解]]，其中包含：
- Function Calling 的完整工作流程
- 5 大常见翻车场景与解法
- Schema 设计最佳实践
- OpenAI vs Anthropic 对比
- 快速排查 Checklist

## 关联页面

- [[concepts/概念_FunctionCalling|Function Calling]] — 工具调用的技术实现细节
- [[concepts/概念_Harness工程|Harness Engineering]] — 工具调用的安全护栏
- [[concepts/概念_AI_Agent|AI Agent]]
- [[concepts/概念_Skill系统|Skill 系统]]
- [[concepts/概念_上下文工程|上下文工程]]
- [[entities/项目_OpenClaw|OpenClaw 项目]]
- [[sources/来源_OpenClaw橙皮书|来源：OpenClaw橙皮书]]
- [[sources/来源_FunctionCalling|来源：Function Calling]]
